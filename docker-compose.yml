# ═══════════════════════════════════════════════════════════════════════════════
# NDCGenie AI - Docker Compose Configuration
# Production deployment with optional services
# ═══════════════════════════════════════════════════════════════════════════════

version: '3.8'

services:
  # Main Application
  NDCGenie:
    build: .
    container_name: NDCGenie-ai
    restart: unless-stopped
    ports:
      - "8501:8501"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
    volumes:
      # Mount for persistent data (optional)
      - NDCGenie_data:/app/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8501/_stcore/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - NDCGenie_network

  # Optional: Nginx reverse proxy for production
  # Uncomment if you need SSL termination or load balancing
  # nginx:
  #   image: nginx:alpine
  #   container_name: NDCGenie-nginx
  #   restart: unless-stopped
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #     - ./ssl:/etc/nginx/ssl:ro
  #   depends_on:
  #     - NDCGenie
  #   networks:
  #     - NDCGenie_network

volumes:
  NDCGenie_data:
    driver: local

networks:
  NDCGenie_network:
    driver: bridge
